# Neovim config
My config currently doesn't use mason which means all lsps specified in variable local servers (that you'd want to use) should be manualy installed (either package manager or manually) and added to path.
